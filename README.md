# Veganistan API service


Create a virtual environment and install the dependencies using ``pip3``::

    mkvirtualenv -p /usr/local/bin/python3 veganistan_api
    pip3 install -r requirements/base.txt

